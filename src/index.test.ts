import { describe, expect, it } from "vitest"

describe("Whatever", () => {
  it("Should pass CI", () => {
    expect(1).toBe(1)
  });
});

// describe("Another", () => {
//   it("Should fail CI", () => {
//     expect(1).toBe(2)
//   });
// });
